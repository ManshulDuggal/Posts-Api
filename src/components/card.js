import styled from "styled-components";

const Card = ({ title }) => {
    return (
        <>
            <h1>this is gonna be my card</h1>
            <h2>{title}</h2>
        </>
    );
};

export default Card;
